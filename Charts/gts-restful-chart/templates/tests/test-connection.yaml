apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gts-restful-chart.fullname" . }}-test-connection"
  labels:
    {{- include "gts-restful-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gts-restful-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
